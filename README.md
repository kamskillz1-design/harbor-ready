# Harbor

Self-guided wellness companion (check-ins, journal, toolkit, goals, companion).

This repo is detached from Base44. Backend is Supabase; frontend deploys on Vercel.

```bash
cp .env.example .env.local
npm install
npm run dev
```

See `DEPLOY.md` for schema, Auth URLs, and Vercel setup.
